function numeroVezesLetra(frase){
    var contadorLetra = [];
    for (var indiceFrase = 0; indiceFrase < frase.length; indiceFrase++){
        contadorLetra.push(frase[indiceFrase])
    }
return contadorLetra;
}
numeroVezesLetra('wilson')
